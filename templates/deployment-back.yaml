{{- if not (or .Values.neoload.configuration.backend.mongo.host .Values.mongodb.existingSecret) }}
  {{- fail "You must set either neoload.configuration.backend.mongo.host or mongodb.existingSecret" }}
{{- end }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "nlweb.fullname" . }}-back
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "nlweb.labels" . | nindent 4 }}
    {{- range $key, $value := .Values.neoload.labels.backend }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
  annotations:
    {{- if and .Values.neoload.annotations .Values.neoload.annotations.backend }}
    {{- toYaml .Values.neoload.annotations.backend | nindent 4 }}
    {{- end }}
    {{- if and .Values.neoload.annotations .Values.neoload.annotations.deployment .Values.neoload.annotations.deployment.backend }}
    {{- toYaml .Values.neoload.annotations.deployment.backend | nindent 4 }}
    {{- end }}
spec:
  replicas: {{ .Values.replicaCount.backend }}
  selector:
    matchLabels:
      {{- include "nlweb.backend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "nlweb.backend.selectorLabels" . | nindent 8 }}
        {{- range $key, $value := .Values.neoload.labels.backend }}
        {{ $key }}: {{ $value | quote }}
        {{- end }}
      annotations:
        {{- if and .Values.neoload.annotations .Values.neoload.annotations.backend }}
        {{- toYaml .Values.neoload.annotations.backend | nindent 8 }}
        {{- end }}
        {{- if and .Values.neoload.annotations .Values.neoload.annotations.pod .Values.neoload.annotations.pod.backend }}
        {{- toYaml .Values.neoload.annotations.pod.backend | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "nlweb.serviceAccountName" . }}
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}-backend
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.backend.repository }}:{{ include "nlweb.backend.imageTag" . }}"
          imagePullPolicy: {{ .Values.image.backend.pullPolicy }}
          env:
            - name: MEMORY_MAX
              value: {{ .Values.neoload.configuration.backend.java.xmx }}
            - name: MONGODB_HOST
            {{- if and .Values.neoload.configuration.backend.mongo.host .Values.mongodb.existingSecret }}
              value: {{ .Values.neoload.configuration.backend.mongo.host | quote }}
            {{- else }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.mongodb.existingSecret | default (include "nlweb.secrets.mongodb.name" .) }}
                  key: host
            {{- end }}
            - name: MONGODB_PORT
              value: {{ default "27017" (.Values.neoload.configuration.backend.mongo.port | quote) }}
            {{- if .Values.mongodb.usePassword }}
            - name: MONGODB_LOGIN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.mongodb.existingSecret | default (include "nlweb.secrets.mongodb.name" .) }}
                  key: username
            - name: MONGODB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.mongodb.existingSecret | default (include "nlweb.secrets.mongodb.name" .) }}
                  key: password
            {{- end }}
            {{- if or .Values.neoload.configuration.backend.licensingPlatformToken .Values.neoload.configuration.backend.licensingPlatformTokenExistingSecret }}
            - name: LICENSING_PLATFORM_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.neoload.configuration.backend.licensingPlatformTokenExistingSecret | default (include "nlweb.secrets.licensing.name" .) }}
                  key: licensingPlatformToken
            {{- end }}
            - name: MONGODB_MAX_POOLSIZE
              value: {{ .Values.neoload.configuration.backend.mongo.poolSize | quote }}
            - name: NEOLOAD_WEB_PUBLIC_URL
              value: {{ printf "%s%s" (include "nlweb.helpers.getScheme" .) (include "nlweb.webapp.host" .) | required "NeoLoad Web Public URL (.Values.services.webapp.host) value is required." | quote }}
            - name: NEOLOAD_WEB_URL_NEXT_GEN
              value: {{ printf "%s%s" (include "nlweb.helpers.getScheme" .) (include "nlweb.webapp.host" .) | required "NeoLoad Web Public URL (.Values.services.webapp.host) value is required." | quote }}
            - name: NEOLOAD_WEB_PUBLIC_URL_NEXT_GEN
              value: {{ printf "%s%s/public" (include "nlweb.helpers.getScheme" .) (include "nlweb.webapp.host" .) | required "NeoLoad Web Public URL (.Values.services.webapp.host) value is required." | quote }}
            - name: NEOLOAD_WEB_API_PUBLIC_URL
              value: {{ printf "%s%s" (include "nlweb.helpers.getScheme" .) (include "nlweb.api.host" .) | required "NeoLoad Web API URL (.Values.services.api.host) value is required." | quote }}
            - name: FILE_STORAGE_ROUTER_BASE_URL
              value: {{ printf "%s%s" (include "nlweb.helpers.getScheme" .) (include "nlweb.files.host" .) | required "NeoLoad Web Files API URL (.Values.services.files.host) value is required." | quote }}
            - name: INTERNAL_FILE_STORAGE_ROUTER_BASE_URL
              value: "http://{{ include "nlweb.fullname" . }}-svc-files.{{ .Release.Namespace }}.svc.cluster.local"              
            - name: FILE_PROJECT_MAX_SIZE_IN_BYTES
              value: {{ int .Values.neoload.configuration.backend.misc.files.maxUploadSizeInBytes | quote }}
            - name: MAX_FORM_ATTRIBUTE_SIZE
              value: {{ int .Values.neoload.configuration.backend.misc.maxFormAttributeSize | quote }}
            - name: NLPROJECT_MAX_UPLOADED_FILES_PER_WEEK
              value: {{ int .Values.neoload.configuration.backend.misc.files.maxUploadPerWeek | quote }}
            - name: SEND_USAGE_STATISTICS
              value: {{ default "true" (.Values.neoload.configuration.sendUsageStatistics | quote) }}
            - name: NLWEB_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.neoload.configuration.secretKeyExistingSecret | default (include "nlweb.secrets.key.name" .) }}
                  key: nlwSecretKey
            - name: INTERNAL_TOKEN_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "nlweb.secrets.key.name" . }}
                  key: internalTokenSecret
            - name: DEPLOYMENT_TYPE
              value: "helm/kubernetes"
            - name: HA
              value: "true"
            - name: START_MODE
              value: "CLUSTER_KUB"
            - name: KUB_NAMESPACE
              value: {{ .Release.Namespace | quote }}
            - name: KUB_HZ_PORT
              value: "6801"
            - name: HAZELCAST_KUBERNETES_SERVICE_PORT
              value: "6801"
            - name: HAZELCAST_KUBERNETES_RESOLVE_NOT_READY_ADDRESSES
              value: "true"
            {{- if eq (include "nlweb.ha.mode" .) "API" }}
            - name: KUB_SERVICE_NAME
              value: "{{ include "nlweb.service-hazelcast-name" . }}"
            {{- end -}}
            {{- if eq (include "nlweb.ha.mode" .) "DNS" }}
            - name: KUB_SERVICE_DNS_NAME
              value: "{{ include "nlweb.service-hazelcast-name" . }}.{{ .Release.Namespace }}.svc.cluster.local"
            {{- end -}}
            {{- if .Values.neoload.configuration.backend.others }}
            {{- range $name, $value := .Values.neoload.configuration.backend.others }}
            - name: {{ $name }}
              value: {{ $value | quote }}
            {{- end }}
            {{- end }}
            - name: CORS_ALLOWED_ORIGIN_PATTERN
              value: {{ include "nlweb.helpers.corsAllowedOriginPattern" . | quote }}
            {{- if or (not .Values.neoload.configuration.backend.others) (not .Values.neoload.configuration.backend.others.COOKIE_DOMAIN) }}
            - name: COOKIE_DOMAIN
              value: {{ (.Values.domain | required "NeoLoad Web domain (.Values.domain) value is required." ) | quote }}
            {{- end }}
          ports:
            - name: api
              containerPort: 1081
              protocol: TCP
            - name: api-v4
              containerPort: 1084
              protocol: TCP
            - name: files
              containerPort: 1082
              protocol: TCP
            - name: backend-admin
              containerPort: 9092
              protocol: TCP
            - name: hazelcast
              containerPort: 6801
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /status
              port: api
            initialDelaySeconds: {{ .Values.neoload.configuration.backend.livenessProbe.initDelaySeconds }}
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /status
              port: api
            initialDelaySeconds: {{ .Values.neoload.configuration.backend.readinessProbe.initDelaySeconds }}
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources.backend | nindent 12 }}
          volumeMounts:
            - name: {{ .Release.Name }}-logger-config
              mountPath: /server/conf
        {{- if ((.Values.extra).volumeMounts).backend }}
            {{- toYaml .Values.extra.volumeMounts.backend | nindent 12 }}
        {{- end }}
        {{- if ((.Values.extra).containers).backend }}
        {{- toYaml .Values.extra.containers.backend | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      volumes:
        - name: {{ .Release.Name }}-logger-config
          configMap:
            name: {{ .Release.Name }}-logger-config
    {{- if ((.Values.extra).volumes).backend }}
        {{- toYaml .Values.extra.volumes.backend | nindent 8 }}
    {{- end }}