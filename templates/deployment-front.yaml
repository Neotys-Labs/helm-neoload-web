apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "nlweb.fullname" . }}-front
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "nlweb.labels" . | nindent 4 }}
    {{- range $key, $value := .Values.neoload.labels.frontend }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
  annotations:
    {{- if and .Values.neoload.annotations .Values.neoload.annotations.frontend }}
    {{- toYaml .Values.neoload.annotations.frontend | nindent 4 }}
    {{- end }}
    {{- if and .Values.neoload.annotations .Values.neoload.annotations.deployment .Values.neoload.annotations.deployment.frontend }}
    {{- toYaml .Values.neoload.annotations.deployment.frontend | nindent 4 }}
    {{- end }}
spec:
  replicas: {{ .Values.replicaCount.frontend }}
  selector:
    matchLabels:
      {{- include "nlweb.frontend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "nlweb.frontend.selectorLabels" . | nindent 8 }}
        {{- range $key, $value := .Values.neoload.labels.frontend }}
        {{ $key }}: {{ $value | quote }}
        {{- end }}
      annotations:
        {{- if and .Values.neoload.annotations .Values.neoload.annotations.frontend }}
        {{- toYaml .Values.neoload.annotations.frontend | nindent 8 }}
        {{- end }}
        {{- if and .Values.neoload.annotations .Values.neoload.annotations.pod .Values.neoload.annotations.pod.frontend }}
        {{- toYaml .Values.neoload.annotations.pod.frontend | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "nlweb.serviceAccountName" . }}
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}-frontend
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.frontend.repository }}:{{ include "nlweb.frontend.imageTag" . }}"
          imagePullPolicy: {{ .Values.image.frontend.pullPolicy }}
          env: 
            - name: PORT
              value: '3000'
            - name: API_URL
              value: {{ printf "%s%s" (include "nlweb.helpers.getScheme" .) (include "nlweb.api-v4.host" .) | required "API v4 URL (.Values.services[\"api-v4\"].host or .Values.services.api.host) value is required." | quote }}
            - name: API_FILE_URL
              value: {{ printf "%s%s" (include "nlweb.helpers.getScheme" .) (include "nlweb.files.host" .) | required "NeoLoad Web Files API URL (.Values.services.files.host) value is required." | quote }}
            - name: FRONT_PUBLIC_URL
              value: {{ printf "%s%s/public" (include "nlweb.helpers.getScheme" .) (include "nlweb.webapp.host" .) | required "NeoLoad Web Next Gen Public URL (.Values.services.webapp.host or .Values.extra.hosts.webapp) value is required." | quote }}
            {{- if .Values.neoload.configuration.frontend.others }}
            {{- range $name, $value := .Values.neoload.configuration.frontend.others }}
            - name: {{ $name }}
              value: {{ $value | quote }}
            {{- end }}
            {{- end }}
          ports:
            - name: webapp
              containerPort: 3000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: webapp
            initialDelaySeconds: {{ .Values.neoload.configuration.frontend.livenessProbe.initDelaySeconds }}
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: webapp
            initialDelaySeconds: {{ .Values.neoload.configuration.frontend.readinessProbe.initDelaySeconds }}
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources.frontend | nindent 12 }}
        {{- if ((.Values.extra).containers).frontend }}
        {{- toYaml .Values.extra.containers.frontend | nindent 8 }}
        {{- end }}
    {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with ((.Values.extra).volumes).frontend }}
      volumes:
        {{- toYaml . | nindent 8 }}
    {{- end }}
